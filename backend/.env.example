# Lokalnie mozesz zostawic ten plik pusty — backend uzyje wtedy SQLite.
# Produkcja (Render) ustawia DATABASE_URL automatycznie przez render.yaml.
PORT=4000
JWT_SECRET=zmien-na-dlugi-losowy-ciag
# DATABASE_URL=postgresql://user:pass@host:5432/baza
